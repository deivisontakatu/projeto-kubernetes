apiVersion: v1
kind: Service

metadata:
  name: projeto-kubernetes-service

spec:
  type: NodePort

  selector:
    app: projeto-kubernetes

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080